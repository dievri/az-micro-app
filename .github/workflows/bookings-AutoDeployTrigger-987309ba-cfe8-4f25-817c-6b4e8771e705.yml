name: Trigger auto deployment for bookings

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/bookings-AutoDeployTrigger-987309ba-cfe8-4f25-817c-6b4e8771e705.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.BOOKINGS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.BOOKINGS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.BOOKINGS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerfilePath: bookings/Dockerfile
          _targetLabelKey_: _targetLabel_
          registryUrl: gallensaintaigo-bqavbdfyg6gucyh6.azurecr.io
          registryUsername: ${{ secrets.BOOKINGS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.BOOKINGS_REGISTRY_PASSWORD }}
          containerAppName: bookings
          resourceGroup: MyGalendewagen
          imageToBuild: gallensaintaigo-bqavbdfyg6gucyh6.azurecr.io/bookings:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


